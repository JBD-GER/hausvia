import { PageHeader, Panel } from "@/components/portal/PortalUI";
import { requireCustomerContext } from "@/lib/portal/access";

export default async function CustomerProfilePage() { const { profile, customer } = await requireCustomerContext(); const row = Array.isArray(customer) ? customer[0] : customer; return <><PageHeader eyebrow="Profil" title={row?.company_name || row?.contact_name || profile.full_name} text="Ihr sicherer Zugang zum Hausvia-Kundenportal." /><Panel title="Kontodaten"><dl className="grid gap-4 text-sm sm:grid-cols-2"><div><dt className="font-bold text-slate-500">Kunde</dt><dd className="mt-1 font-black text-slate-950">{row?.company_name || row?.contact_name}</dd></div><div><dt className="font-bold text-slate-500">E-Mail</dt><dd className="mt-1 font-black text-slate-950">{profile.email}</dd></div><div><dt className="font-bold text-slate-500">Rolle</dt><dd className="mt-1 font-black text-slate-950">Kunde</dd></div><div><dt className="font-bold text-slate-500">Status</dt><dd className="mt-1 font-black text-slate-950">Aktiv</dd></div></dl></Panel></>; }
